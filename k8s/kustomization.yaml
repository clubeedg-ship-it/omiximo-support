apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secrets are intentionally NOT listed here — create them out-of-band so values
# never land in git. See secret.example.yaml and README.md.
resources:
  - 00-namespace.yaml
  - 10-db.yaml
  - 20-api.yaml
  - 30-frontend.yaml
